#!/usr/bin/env python3
import sys, os, getpass
import math, random
import functools, itertools, collections, heapq, bisect
from collections import Counter, defaultdict, deque

MAXINT = sys.maxsize


# generate random tree

# generate the standard trees
# line
# hub and spoke

# generate random array
